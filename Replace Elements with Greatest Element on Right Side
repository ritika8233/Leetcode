class Solution {
public:
    vector<int> replaceElements(vector<int>& a) {
        int n=a.size();
        int temp=-1,p;
        for(int i=n-1;i>=0;i--){
            p=a[i];
            a[i]=temp;
            temp=max(temp,p);
        }
        return a;
    }
};
